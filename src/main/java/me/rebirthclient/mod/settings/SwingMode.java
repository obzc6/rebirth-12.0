package me.rebirthclient.mod.settings;

public enum SwingMode {
    Normal,
    Client,
    Server,
    None;

    // $FF: synthetic method
    private static SwingMode[] $values() {
        return new SwingMode[]{Normal, Client, Server, None};
    }
}
